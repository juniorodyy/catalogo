<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
   <div class="field">
    <%= f.label :Genero %><br>
    <%= f.select :Genero, [["Ação",1], ["Drama",2],["Terror",3],["Comédia",4],["Romance",5],["Suspense",6]] %>
  </div>
<div class="field">
    <%= f.label :group_id %><br>
    <%= f.select :group_id, Group.all.collect { |group| [group.name, group.id] } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
